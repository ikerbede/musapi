from django.core import serializers
from django.http import JsonResponse

# TODO
# /users --> all users
# /users/:userId --> one user
# /users/:userId/messages --> all messages of one user
# /users/:userId/rounds --> all rounds of one user (includes scores)
# /users/:userId/rounds/:roundId --> one round of one user (includes score)


